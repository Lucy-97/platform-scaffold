# {{.Brand}} 项目开发规约

> 本文是给 AI 助手 + 开发者的「Non-Negotiable Rules」。开始改任何代码前先读完。

## 1. 系统结构

四类进程，五条边界：

| 进程 | 端口 | 职责 | 写权限 |
|------|------|------|--------|
| `backend-gateway` (Go) | {{.Ports.Gateway}} | JWT 鉴权、限流、CORS、把请求按路径前缀转发 | 否（只代理） |
| `backend-api` (Go) | {{.Ports.API}} | 三层（handler→service→repository）业务逻辑 | **是（唯一写库者）** |
{{- if .Features.AIEngine }}
| `backend-ai-engine` (Python) | {{.Ports.AIEngine}} | AI 编排、LLM/Tool 调度 | 否（只读 + 委托 Go API 写） |
{{- end }}
{{- if .Features.Web }}
| `frontend-web` (Next.js) | {{.Ports.Web}} | C 端页面 | — |
{{- end }}
{{- if .Features.Admin }}
| `frontend-admin` (Vite) | {{.Ports.Admin}} | 后台管理 | — |
{{- end }}

## 2. 写权限矩阵（最重要的规约）

> ⚠️ **任何 DB 写操作只能由 `backend-api` 执行**，不允许 Python 端直接写库。

- Python AI Engine 需要落账/扣费时，通过 `httpx` 调内网 `http://api:{{.Ports.API}}/internal/...`，并在头里带 `X-Internal-Secret`。
- `backend-api/internal/handler/internal_*.go` 是这些内网接口的接收端，外网经 Gateway 进不来。
- Gateway 不直接连数据库，只做鉴权/限流/转发。

违反这条规则会导致：双写不一致、扣费幂等性丢失、审计日志断层。

## 3. 四域路由原则（Gateway → 下游）

| 路径前缀 | 转发目标 | 备注 |
|---------|---------|------|
| `/api/auth/*`, `/api/public/*` | `backend-api` | 公开路径，无需 JWT |
| `/api/v1/*` (其他) | `backend-api` | 需 JWT；Gateway 注入 `X-User-UUID` 头 |
{{- if .Features.AIEngine }}
| `/api/v1/ai/*` | `backend-ai-engine` | 需 JWT；同样注入用户头 |
{{- end }}
| `/health`, `/metrics` | Gateway 自身 | 不转发 |

新增业务路由时：
1. 在 [backend-gateway/internal/router/routes.go](file:///backend-gateway/internal/router/routes.go) 注册前缀
2. 在对应下游服务实现 handler

## 4. 三层配置管理

```
                  ┌──────────────────────┐
   编译期常量  →  │ pkg/const            │  ← 不变的业务常量（错误码、枚举值）
                  └──────────────────────┘
                  ┌──────────────────────┐
   启动期配置  →  │ .env / configmap     │  ← 服务地址、密钥、端口
                  └──────────────────────┘
                  ┌──────────────────────┐
   运行期配置  →  │ system_config 表     │  ← 可热更新（费率、模型名、开关）
                  └──────────────────────┘
```

- 启动期配置走 [internal/config/config.go](file:///backend-api/internal/config/config.go)，进程启动一次性加载。
- 运行期配置走 [pkg-platform-core/dynconfig](file:///pkg-platform-core/dynconfig)，5 分钟 Redis 缓存 + 数据库回源。
- **敏感配置（API Key、第三方 secret）必须 `encrypted=1`**，AES-256-GCM 加密，主密钥由 `CONFIG_MASTER_KEY` 环境变量注入。

## 5. 错误处理约定

- 业务错误码用 6 位数字，注册到 [pkg-platform-core/errcode](file:///pkg-platform-core/errcode)
- HTTP 200 + `{code, msg, data}` 是默认响应；HTTP 401/403/429/500 仅用于基础设施层（鉴权失败、限流、未捕获 panic）
- 所有错误码必须挂在 errcode registry 上，便于前端统一国际化

## 6. 服务间鉴权

```
Gateway --[X-Internal-Secret + X-User-UUID]--> API / AI-Engine
```

- `INTERNAL_API_SECRET` 环境变量在三个服务间共享。
- 中间件 [pkg-platform-core/middleware.InternalAuth](file:///pkg-platform-core/middleware/middleware.go) 负责校验。
- 校验用 `subtle.ConstantTimeCompare`，防时序攻击。
- **dev 环境** `INTERNAL_API_SECRET=""` 时跳过校验；prod 必须设置。

## 7. 不可妥协的规则

1. ❌ Python AI Engine 直接 `INSERT/UPDATE/DELETE` 数据库
2. ❌ 在前端硬编码 `http://api:port`，所有请求走 Gateway
3. ❌ JWT_SECRET / INTERNAL_API_SECRET / CONFIG_MASTER_KEY 出现在代码或 git 提交里
4. ❌ 给 [pkg-platform-core](file:///pkg-platform-core) 引入业务依赖（它必须保持业务无关）
5. ❌ 跳过 errcode 注册直接 `c.JSON(http.StatusBadRequest, ...)`
6. ❌ 在生产部署里把 `system_config.encrypted=0` 用于存储 API Key

## 8. 新增模块的标准流程

新增一个业务模块（以 `order` 为例）：

```
backend-api/internal/
├── model/order.go              # GORM model
├── repository/order_repo.go    # DB 访问
├── service/order_service.go    # 业务逻辑
└── handler/order.go            # HTTP handler
```

然后：
1. 在 [router/routes.go](file:///backend-api/internal/router/routes.go) 挂路由
2. 在 [bootstrap.go](file:///backend-api/internal/app/bootstrap.go) 装配 DI
3. 在 [errcode/errcode.go](file:///pkg-platform-core/errcode/errcode.go) 注册新错误码（10xxxx）
4. 数据库变更写到 `database/migrations/`，**禁止直接改 init.sql**

## 9. 测试

- Go 单测：`go test ./...`
- pkg-platform-core 必须独立可测：`cd pkg-platform-core && go test ./...`
- 集成测试用 docker-compose 起依赖

---

如有规约更新，请同时更新本文件 + 团队 Wiki。
