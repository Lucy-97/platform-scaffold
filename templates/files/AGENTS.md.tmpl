# Agent 通用工程规范

本文件定义编码 agent 的通用工作准则，可复制到不同代码仓库使用。项目架构、运行方式、文档索引和领域规则应放在各仓库的 `README.md` 或项目文档中。

## 工作方式

- 开始任务前先阅读 `README.md`，理解项目结构、运行方式和边界约束。
- 优先做最小、聚焦的改动；未经明确要求，不做跨模块重构、不顺手修无关问题。
- 发现无关的未提交改动时，不回滚、不覆盖、不格式化；只处理当前任务相关文件。
- 面向用户的说明、计划、总结和文档默认使用中文；代码、API 名称和技术术语可保留英文。
- 每次任务完成后，用 `git add` 暂存本次相关文件并执行 `git commit`；提交信息的标题和正文默认使用中文总结，可保留必要的英文技术名词，且需包含详细变更说明，方便用户在 IDE Source Control 中 review 差异。

## 代码质量

- 遵循现有架构、目录组织、命名、错误处理和日志风格。
- 新增抽象必须有明确收益：减少真实重复、隔离复杂度或匹配既有模式。
- 禁止以局部打补丁的方式堆叠代码来绕开结构问题，尤其是 Python 代码不得通过新增一批零散函数持续堆砌复杂度；遇到设计或职责失衡时，应进行必要的结构性重构。
- 当单个代码文件超过 500 行时，应开始评估拆分与重构；禁止让单个代码文件超过 1000 行，确有历史遗留时新增改动必须优先降低文件规模或迁移职责。
- 公共接口、边界层和异步/并发路径必须显式处理错误、超时、取消和重试边界。
- 新增生产依赖前说明理由，并考虑 license、CVE、包体积和维护风险。

## 数据与安全

- 遵守项目既有的数据 ownership；不要绕过服务边界直接读写不属于当前模块的数据。
- 数据库 schema 变更必须提供可重复执行的 migration；初始化脚本只能作为新环境基线。
- 如项目维护初始化基线脚本，新增或修改 migration 时必须同步更新基线，确保新环境初始化后的 schema 与按历史 migrations 升级后的 schema 一致。
- 禁止在 schema migration 中播种业务配置、密钥占位、提示词、商品、价格、政策版本等业务数据；默认配置和 mock 数据应按项目文档指定的数据初始化流程维护。
- 禁止硬编码 secrets、API key、令牌、内部地址和环境相关配置。
- 环境变量来源、示例文件和注入方式以项目文档为准；不要在未约定的位置散落新的 `.env*` 文件。
- 公共 API 必须做输入校验、鉴权/权限判断，并考虑限流与滥用风险。
- 涉及金额、余额、库存、积分、解锁、提现等并发写场景时，必须使用事务和分布式锁或项目认可的等价机制。

## Go

- 保持清晰分层；HTTP handler 只负责解析、校验和响应，不直接承载业务编排或数据访问。
- 依赖变更后在对应 Go module 内运行 `go mod tidy`。
- 并发写、缓存 key、TTL、内部接口鉴权等遵循项目已有实现。
- 修改 Go 服务后，按项目文档运行对应的 reload、测试或构建命令，确保运行中的进程加载新二进制。

## Python

- 使用 Python 3.12。
- 异步路径保持全链路异步，禁止 `time.sleep()` 和同步阻塞 I/O。
- DB、Redis、HTTP client 等连接池应在应用生命周期内复用，禁止按请求创建。
- Schema、配置、依赖注入按项目既有位置和风格组织；配置通过环境或配置系统读取。

## 前端

- 新增前端代码优先 TypeScript；客户端组件、服务端组件、SSR/CSR 边界按框架约定显式处理。
- API 调用、状态管理、表单校验、样式变量和组件库优先复用项目既有封装。
- 非实时数据优先考虑 SSR/SSG/ISR；交互态、会话态、实时态再使用 CSR。
- 前端本地服务的启动方式、端口和验证策略以项目文档为准；Agent 不应擅自持有长驻 dev server，如用户已手动启动本地前端，Agent 可复用对应端口做页面验证。
- UI 改动必须检查移动端和桌面端的布局、溢出、空状态、加载态和错误态。
- 处理弹窗、面板、网格、滚动容器、红框截图空白/溢出/裁切等前端布局问题时，先阅读并遵循 `.agents/skills/frontend-layout-qa/SKILL.md`。
- 前端页面、交互、布局和端到端问题，优先使用项目已配置的 Playwright 或 Chrome/Chromium headless 能力进行截图、DOM 检查和 E2E 复现。
- Next.js 项目中，禁止在同一个工作区同时运行 `next dev` 和 `next build` / `npm run build`，因为它们会共同写入 `.next` 并导致 chunk/runtime 缓存产物不一致（例如 `Cannot find module './124.js'`）。需要生产构建验证时，必须先确认并停止 dev server，或使用隔离的工作区/输出目录；只做类型验证时优先运行 `tsc --noEmit`。如果已出现 `.next` chunk 缺失或 runtime 路径错乱，先删除 `.next`，再重新启动 dev server。


## 测试与验证

- 新端点、复杂业务逻辑、并发写路径、复杂组件应补测试；不补测试需说明原因。
- Go 使用 `go test`，Python 使用 `pytest`，前端使用项目配置的 lint/build/test/E2E 命令。
- 改动后用搜索、差异检查、单元测试、构建或手工验证确认关键行为已生效。
- 运行 Go 测试前先判断当前沙箱是否允许写入默认 Go build cache（如 `~/Library/Caches/go-build`）；若不允许且未显式配置仓库内 `GOCACHE`，应直接对对应 `go test` 命令请求最小范围提权，避免先执行一次必然失败的测试。

## 修改卫生

- 搜索文本和文件优先使用 `rg` / `rg --files`。
- 手工编辑文件使用可靠的 patch/edit 工具；不要用临时脚本、`sed -i`、heredoc 覆盖源码。
- 不在仓库中遗留 `patch_*.py`、临时 `test_*`、`*.bak`、`*.orig`、`*.tmp` 等调试产物。
- 不运行破坏性命令，例如 `git reset --hard`、`git checkout -- <file>`、无明确目标的 `rm`，除非用户明确要求并确认。
- 运行 git 命令前先用 `git status --short` 和必要的 `git diff --stat` 确认工作区；不要使用 `git add .`，只暂存本次任务相关的明确路径。
- 路径中含有 glob 特殊字符时必须加引号，避免 shell 展开导致命令失败；尤其是 Next.js 动态路由目录如 `[lang]`、`[id]`、`[[...slug]]`，在 `zsh` 中必须写成 `'frontend/web/app/[lang]/...'` 这类字面路径。
- 提交前用 `git diff --cached --stat` 复核暂存区；如果暂存区已有无关改动，不要用 `git reset`/`checkout` 回滚用户改动，改用 `git commit -- <path...>` 只提交本次相关文件。
- 必须要求对 `git add` / `git commit` 请求最小范围提权，因为沙箱是无法写入 `.git/index.lock` 或提交对象的！；不要为了绕过限制改写文件或移动 `.git`。
- 运行命令前先识别它会写入的位置；对已知会写入仓库外缓存、Docker socket、包管理器缓存或 `.git` 元数据且当前沙箱不允许的必要命令，直接使用最小范围提权，并在总结中说明原因。不要为了绕过沙箱限制改写缓存目录、移动 `.git` 或改变项目约定的执行方式。
