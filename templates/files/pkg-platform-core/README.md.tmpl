# pkg-platform-core

技术共享平台公共组件库。从 xdd / aigc 两个生产项目中沉淀出来的稳定组件，作为独立 Go module 维护。

## 包列表

| 包 | 职责 |
|----|------|
| `errcode` | 6 位业务错误码注册表，对接 BizException 风格 |
| `lock` | Redis 分布式锁（SETNX + Lua 原子释放） |
| `cache` | Cache-Aside 泛型缓存封装 |
| `dynconfig` | 动态配置加载器（system_config 表 + AES-GCM 解密） |
| `crypto` | AES-256-GCM 加解密（与 Python 端 SHA-256 派生密钥对齐） |
| `response` | 统一响应格式 `{code, msg, data}` |
| `middleware` | 通用 Gin 中间件：jwt / internal_auth / request_id / cors / metrics / ratelimit |

## 设计原则

1. **零业务依赖**：所有包不引用业务 model，只依赖 stdlib + redis-go + gin + gorm 等通用基础设施。
2. **可独立测试**：每个包都有 `_test.go`，跑通基本路径。
3. **优雅降级**：Redis 不可用时限流 fail-open，DEK 缺失时只 warn 不阻断启动。

## 引用方式

新项目通过 `go.mod` 的 `replace` 指向同仓库：

```
replace {{.GoModulePath}}/pkg-platform-core => ../pkg-platform-core
```
