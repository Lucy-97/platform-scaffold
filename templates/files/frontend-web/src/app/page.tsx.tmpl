const mainStyle = { padding: 32, fontFamily: "system-ui" } as const;

export default function HomePage() {
  return (
    <main style={mainStyle}>
      <h1>{{.Brand}}</h1>
      <p>Generated by platform-scaffold. Edit <code>src/app/page.tsx</code> to start.</p>
      <ul>
        <li>Gateway: http://localhost:{{.Ports.Gateway}}</li>
        <li>API:     http://localhost:{{.Ports.API}}</li>
{{- if .Features.AIEngine }}
        <li>AI:      http://localhost:{{.Ports.AIEngine}}</li>
{{- end }}
      </ul>
    </main>
  );
}
